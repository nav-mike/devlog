---
// Define the component's props interface
interface Props {
  text: string;
  className?: string;
}

// Destructure props with default values
const { text, className = '' } = Astro.props;
---

<h2 class={`text-2xl font-bold text-gray-800 dark:text-white mb-4 mt-8 ${className}`}>
  {text}
</h2>